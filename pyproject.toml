[project]
name = "kivola-ai-adoption"
version = "0.1.0"
description = "Reduced roadmap pipeline for the Kivola AI adoption case."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
